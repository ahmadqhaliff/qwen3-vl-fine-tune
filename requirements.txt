# Core: training + merge + quantize
torch
transformers
peft
accelerate
datasets
safetensors
pillow
tqdm

# QLoRA base loading (load_in_4bit=True)
bitsandbytes>=0.46.1

# Optional: JSON Schema validation (stronger than the built-in checks)
# jsonschema>=4.23.0
